# Unified Test Taxonomy

This folder mirrors the recommended top-level automation taxonomy:

- `e2e/`
- `api/`
- `unit/`
- `integration/`
- `fixtures/`
- `utils/`

Execution-ready implementation lives in:
- `frontend/tests/*`
- `backend/tests/*`

This split keeps tool-specific configs local while preserving a consistent architecture.
